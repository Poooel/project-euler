package launcher;

public interface Executable {
    /**
     * Execute the choosen problem and return the result as a String
     * @return The result as a String for the choosen problem
     */
    long execute();
}
